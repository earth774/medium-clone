# Database
# PostgreSQL connection string (local development)
DATABASE_URL="postgresql://user:password@localhost:5432/medium_clone?schema=public"

# For SQLite (easier local setup):
# DATABASE_URL="file:./dev.db"

# Authentication
# Generate a secure random string (at least 32 characters)
# You can generate one with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here-minimum-32-characters-long"

# Next.js
# Optional: Set for explicit configuration
# NEXT_PUBLIC_API_URL="http://localhost:3000"
